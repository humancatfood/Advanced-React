type User {
  id: ID! @unique
  name: String!
  email: String! @unique
}

type Item {
  id: ID! @unique
  title: String!
  description: String!
  price: Int!
  image: String
  largeImage: String
  createdAt: DateTime!
  updatedAt: DateTime!
  # user: User!
}

# type Mutation {
#   createItem (item: Item): Item
# }
